# Frontend Type Definitions

This folder holds TypeScript interfaces and type definitions used across the frontend application.

## Recommended Layout

* **`gestures.ts`**: Types for hand coordinates, pinch status, landmarks, and tracked gesture types.
* **`api.ts`**: Responses for health status, authentication, canvas configurations, and analytics logs.
* **`canvas.ts`**: Canvas drawing paths, shapes, drawing configuration types.
